name: Hello Actions

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  hello-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "Hello, MLOps with GitHub Actions!"
